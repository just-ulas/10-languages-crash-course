use std::f64::consts::PI;

fn main() {
    println!("{}", 16f64.sqrt());
    println!("{}", PI);

    // random needs external crate usually, keep simple
    println!("{}", "use rand crate for random numbers");
}
