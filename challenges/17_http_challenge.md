# Challenge: HTTP

Make a GET request to `https://httpbin.org/get`

Print:
- status code
- one header
- a piece of the JSON body

Keep error handling simple but present.

Python (requests), JavaScript (fetch), Go (net/http).
