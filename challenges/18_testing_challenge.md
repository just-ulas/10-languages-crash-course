# Challenge: Testing

Write a function `is_palindrome(s)` that returns true if a string reads the same forwards and backwards.

Then write at least 3 tests:
- empty string → true
- "radar" → true
- "hello" → false

Run them without a heavy framework if possible (self-check is fine).
