# Challenge: JSON

Create a person object:
```json
{
  "name": "ulas",
  "age": 20,
  "skills": ["python", "go", "rust"]
}
```

1. Convert it to a JSON string (pretty printed)
2. Parse it back
3. Print the second skill

Implement in Python, JS and Go.
