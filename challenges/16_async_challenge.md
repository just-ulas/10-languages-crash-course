# Challenge: Async / Concurrency

Simulate 3 tasks that take different times (e.g. 1s, 0.5s, 0.2s).

Run them concurrently and print when each finishes.

Then print "all done".

- Python → asyncio
- JavaScript → Promise.all / async await
- Go → goroutines + WaitGroup
