# Challenge: Classes / Structs

Create a `Person` with:
- name
- age
- a method/function `greet()` that returns `"hi, i am <name>"`

Create one instance and print the greeting + age.

Compare:
- Python / JS / Java / C# class style
- Go struct + method
- Rust struct + impl
