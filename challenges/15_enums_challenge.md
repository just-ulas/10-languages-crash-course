# Challenge: Enums

Create an enum for HTTP methods: GET, POST, PUT, DELETE.

Write a function that takes a method and prints what it is typically used for.

Example:
- GET → "read data"
- POST → "create data"

Do it in at least Python, TypeScript, Go and Rust.
